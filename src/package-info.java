import javax.swing.*;
class info {
    public void main(String args[]){
        JFrame frame=new JFrame();
        JButton button=new JButton();
        button.setBounds(140,200,100,100);
        frame.add(button);
        frame.setSize(500,500);
        frame.setVisible(true);
    }
}